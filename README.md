# Atlas 0.10.9

UI/layout update. Replace `index.html`, `sw.js`, and `manifest.webmanifest` in the GitHub Pages repository root.

Changes: orbital/force graph layout, aspect-aware map camera, stronger neutral nodes, unified right-side control cluster, auto-closing Tools/System menus, and genuinely draggable floating tool pane. No data schema change.
