# Atlas v0.11.3

UI/behaviour update: contextual Predict projections, centre-bottom widget lane, and repaired MEL/UTC clock placement.

Replace `index.html`, `sw.js`, and `manifest.webmanifest` in the GitHub Pages repo. Existing Atlas data schema remains v6.
