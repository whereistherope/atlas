const fs=require('fs');
const assert=require('assert');
const tokens=fs.readFileSync('styles/tokens.css','utf8');
const overviewCss=fs.readFileSync('styles/network-overview.css','utf8');
const overview=fs.readFileSync('js/network-overview.js','utf8');
const capture=fs.readFileSync('js/capture-flow-fix.js','utf8');
const theme=fs.readFileSync('styles/theme-system.css','utf8');
const hotfix=fs.readFileSync('styles/r18-stability-hotfix.css','utf8');
const bootstrap=fs.readFileSync('js/bootstrap.js','utf8');
const sw=fs.readFileSync('sw.js','utf8');

assert(tokens.includes('--bg:#070b10'),'historical Atlas night palette must remain available beneath the refinement layer');
assert(tokens.includes('html:not(.atlas-ready)'),'minimal no-flash boot guard must remain');
assert(!bootstrap.includes('ops-refinement.css'),'must not load rejected Ops visual overrides');
assert(!bootstrap.includes('window-material.css'),'must not load rejected experimental material layer');
assert(!bootstrap.includes('ops-r5.css'),'must not load rejected r5 palette layer');
assert(bootstrap.includes("loadStyle('./styles/network-overview.css')"),'network overview style must load');
assert(bootstrap.includes("loadStyle('./styles/theme-system.css')"),'approved native refinement must load');
assert(bootstrap.includes("loadStyle('./styles/r18-stability-hotfix.css')"),'r18 stability hotfix must remain loaded after the refinement layer');
assert(!bootstrap.includes("loadScript('./js/window-drag.js'"),'global movable-window runtime must remain disabled');
assert(bootstrap.includes('const SYNC_V2_ENABLED=false'),'automatic record-level sync must be explicitly paused during recovery');
assert(bootstrap.includes("if(SYNC_V2_ENABLED)"),'sync runtime must be behind the emergency recovery gate');
assert(bootstrap.includes("loadScript('./js/sync-v2.js','Atlas record-level sync')"),'sync runtime path must remain available behind the gate');
assert(bootstrap.includes("paused:true"),'paused sync status must be exposed to the UI');
assert(bootstrap.includes("loadScript('./js/capture-flow-fix.js','Atlas capture launcher handoff v0.16.9')"),'capture handoff must remain');
assert(bootstrap.includes("loadScript('./js/network-overview.js','Atlas network overview v0.16.9')"),'network overview must remain');
assert(bootstrap.includes("loadScript('./js/sync-v2-core.js','Atlas record-level sync core')"),'record-level sync core must remain available');
assert(!bootstrap.includes("loadScript('./js/cloud-sync.js'"),'legacy whole-snapshot sync must not boot');
assert(!bootstrap.includes("loadScript('./js/cloud-sync-hotfix.js'"),'legacy snapshot hotfix must not boot');
assert(bootstrap.includes("document.documentElement.classList.add('atlas-ready')"),'boot guard must release after load');
assert(capture.includes('[data-atlas-aligned-capture]'),'capture launcher handoff contract missing');
assert(overview.includes('AtlasNetworkOverview'),'network overview runtime contract missing');
assert(!overview.includes('dataset.skin'),'network overview must not own skin state');
assert(overviewCss.includes('.atlas-network-minimap'),'network overview presentation missing');
assert(theme.includes('#atlasCaptureLauncher'),'production theme must explicitly style Capture glass');
assert(theme.includes('--atlas-float-glass'),'production theme must expose floating glass material');
assert(theme.includes('.quick-todo span'),'To-do contrast fix must remain');
assert(theme.includes('.atlas-physics-panel'),'network controls must share the glass system');
assert(hotfix.includes('box-shadow:none!important'),'decorative pane shadows must remain removed');
assert(sw.includes("atlas-shell-0.16.9-r19"),'recovery cache version missing');
assert(sw.includes("'./styles/theme-system.css'"),'native refinement CSS must be cached');
assert(sw.includes("'./styles/r18-stability-hotfix.css'"),'stability hotfix CSS must be cached');
assert(!sw.includes("'./js/window-drag.js'"),'disabled movable-window runtime must not remain in APP_SHELL');
assert(sw.includes("'./styles/network-overview.css'"),'network overview CSS must be cached');
assert(sw.includes("'./js/network-overview.js'"),'network overview runtime must be cached');
assert(sw.includes("'./js/capture-flow-fix.js'"),'capture fix must be cached');
assert(sw.includes("'./js/sync-v2-core.js'"),'record-level sync core must be cached');
assert(sw.includes("'./js/sync-v2.js'"),'sync runtime may remain cached even though boot is paused');
assert(!sw.includes("'./js/cloud-sync.js'"),'legacy snapshot sync must not be in the app shell');
assert(!sw.includes("'./js/cloud-sync-hotfix.js'"),'legacy snapshot hotfix must not be in the app shell');
assert(!sw.includes("'./assets/lock-terrain.gif'"),'removed lock artwork must not remain offline-critical');

console.log('Atlas v0.16.9-r19 recovery sync pause compatibility: PASS');
